{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Routine Configuration Schema",
  "description": "Schema for data.json configuration file",
  "type": "object",
  "required": ["tasks", "routines", "routineTasks", "users", "settings", "routineAssignments", "flows", "schedules", "rewards"],
  "additionalProperties": false,
  "properties": {
    "tasks": {
      "type": "array",
      "description": "Library of reusable tasks",
      "items": {
        "type": "object",
        "required": ["id", "title", "icon", "stars"],
        "additionalProperties": false,
        "properties": {
          "id": {
            "type": "string",
            "pattern": "^[a-zA-Z0-9-_]+$",
            "description": "Unique task identifier"
          },
          "title": {
            "type": "string",
            "minLength": 1,
            "description": "Display name for the task"
          },
          "icon": {
            "type": "object",
            "required": ["type", "value"],
            "additionalProperties": false,
            "properties": {
              "type": {
                "type": "string",
                "enum": ["emoji", "image"],
                "description": "Icon type: emoji (Unicode character) or image (uploaded file from /uploads)"
              },
              "value": {
                "type": "string",
                "minLength": 1,
                "description": "Emoji character or icon name"
              }
            }
          },
          "stars": {
            "type": "integer",
            "minimum": 0,
            "description": "Star reward for completing task"
          }
        }
      }
    },
    "routines": {
      "type": "array",
      "description": "Template routines (e.g., Morning, Evening)",
      "items": {
        "type": "object",
        "required": ["id", "title", "themeColor", "icon"],
        "additionalProperties": false,
        "properties": {
          "id": {
            "type": "string",
            "pattern": "^[a-zA-Z0-9-_]+$",
            "description": "Unique routine identifier"
          },
          "title": {
            "type": "string",
            "minLength": 1,
            "description": "Display name for the routine"
          },
          "themeColor": {
            "type": "string",
            "description": "CSS color value or var()"
          },
          "icon": {
            "type": "object",
            "required": ["type", "value"],
            "additionalProperties": false,
            "properties": {
              "type": {
                "type": "string",
                "enum": ["emoji", "image"],
                "description": "Icon type: emoji or image (uploaded filename)"
              },
              "value": {
                "type": "string",
                "minLength": 1,
                "description": "Emoji character or uploaded filename (auto-resolves to /uploads/)"
              }
            }
          }
        }
      }
    },
    "routineTasks": {
      "type": "array",
      "description": "Join table mapping routines to tasks with order and duration",
      "items": {
        "type": "object",
        "required": ["id", "routineId", "taskId", "order", "durationSeconds"],
        "additionalProperties": false,
        "properties": {
          "id": {
            "type": "string",
            "pattern": "^[a-zA-Z0-9-_]+$",
            "description": "Unique routine-task mapping identifier"
          },
          "routineId": {
            "type": "string",
            "description": "Reference to routine ID"
          },
          "taskId": {
            "type": "string",
            "description": "Reference to task ID"
          },
          "order": {
            "type": "integer",
            "minimum": 1,
            "description": "Sequence order within routine"
          },
          "durationSeconds": {
            "type": "integer",
            "minimum": 1,
            "description": "Time limit for task completion"
          }
        }
      }
    },
    "users": {
      "type": "array",
      "description": "Children profiles",
      "items": {
        "type": "object",
        "required": ["id", "name", "avatar", "color"],
        "additionalProperties": false,
        "properties": {
          "id": {
            "type": "string",
            "pattern": "^[a-zA-Z0-9-_]+$",
            "description": "Unique user identifier"
          },
          "name": {
            "type": "string",
            "minLength": 1,
            "description": "Child's display name"
          },
          "avatar": {
            "type": "object",
            "required": ["type", "value"],
            "additionalProperties": false,
            "properties": {
              "type": {
                "type": "string",
                "enum": ["emoji", "image"],
                "description": "Avatar type: emoji or image (uploaded filename)"
              },
              "value": {
                "type": "string",
                "minLength": 1,
                "description": "Emoji character or uploaded filename (auto-resolves to /uploads/)"
              }
            }
          },
          "color": {
            "type": "string",
            "description": "CSS color value or var()"
          }
        }
      }
    },
    "settings": {
      "type": "object",
      "required": ["timezone"],
      "additionalProperties": false,
      "properties": {
        "timezone": {
          "type": "string",
          "pattern": "^[A-Za-z]+/[A-Za-z_]+$",
          "description": "IANA timezone identifier (e.g., Europe/Athens)"
        }
      }
    },
    "routineAssignments": {
      "type": "array",
      "description": "User-specific routine instances",
      "items": {
        "type": "object",
        "required": ["id", "userId", "routineId"],
        "additionalProperties": false,
        "properties": {
          "id": {
            "type": "string",
            "pattern": "^[a-zA-Z0-9-_]+$",
            "description": "Unique assignment identifier"
          },
          "userId": {
            "type": "string",
            "description": "Reference to user ID"
          },
          "routineId": {
            "type": "string",
            "description": "Reference to routine ID"
          }
        }
      }
    },
    "flows": {
      "type": "array",
      "description": "Multi-step sequences (alarm + parallel routines)",
      "items": {
        "type": "object",
        "required": ["id", "steps"],
        "additionalProperties": false,
        "properties": {
          "id": {
            "type": "string",
            "pattern": "^[a-zA-Z0-9-_]+$",
            "description": "Unique flow identifier"
          },
          "steps": {
            "type": "array",
            "minItems": 1,
            "items": {
              "oneOf": [
                {
                  "type": "object",
                  "required": ["type", "props"],
                  "additionalProperties": false,
                  "properties": {
                    "type": {
                      "type": "string",
                      "enum": ["alarm"]
                    },
                    "props": {
                      "type": "object",
                      "required": ["sound"],
                      "additionalProperties": false,
                      "properties": {
                        "sound": {
                          "oneOf": [
                            {
                              "type": "string",
                              "enum": ["melody", "beep"],
                              "description": "Built-in sound: melody (wake-up tune) or beep (simple alarm)"
                            },
                            {
                              "type": "object",
                              "required": ["type", "value"],
                              "additionalProperties": false,
                              "properties": {
                                "type": {
                                  "type": "string",
                                  "enum": ["upload"],
                                  "description": "Custom uploaded audio file"
                                },
                                "value": {
                                  "type": "string",
                                  "minLength": 1,
                                  "description": "Uploaded filename (from /uploads/)"
                                }
                              }
                            }
                          ]
                        }
                      }
                    }
                  }
                },
                {
                  "type": "object",
                  "required": ["type", "actions"],
                  "additionalProperties": false,
                  "properties": {
                    "type": {
                      "type": "string",
                      "enum": ["parallel"]
                    },
                    "actions": {
                      "type": "array",
                      "minItems": 1,
                      "items": {
                        "oneOf": [
                          {
                            "type": "object",
                            "required": ["type", "userId", "routineId"],
                            "additionalProperties": false,
                            "properties": {
                              "type": {
                                "type": "string",
                                "enum": ["routine"]
                              },
                              "userId": {
                                "type": "string",
                                "description": "Reference to user ID"
                              },
                              "routineId": {
                                "type": "string",
                                "description": "Reference to routine assignment ID"
                              }
                            }
                          },
                          {
                            "type": "object",
                            "required": ["type", "flowId"],
                            "additionalProperties": false,
                            "properties": {
                              "type": {
                                "type": "string",
                                "enum": ["flow"]
                              },
                              "flowId": {
                                "type": "string",
                                "description": "Reference to flow ID"
                              }
                            }
                          }
                        ]
                      }
                    }
                  }
                }
              ]
            }
          }
        }
      }
    },
    "schedules": {
      "type": "array",
      "description": "Cron-based triggers",
      "items": {
        "type": "object",
        "required": ["id", "cron", "type", "targetId"],
        "additionalProperties": false,
        "properties": {
          "id": {
            "type": "string",
            "pattern": "^[a-zA-Z0-9-_]+$",
            "description": "Unique schedule identifier"
          },
          "cron": {
            "type": "string",
            "pattern": "^[\\d\\*\\-,/]+ [\\d\\*\\-,/]+ [\\d\\*\\-,/]+ [\\d\\*\\-,/]+ [\\d\\*\\-,/]+$",
            "description": "Cron expression (5 fields: minute hour day month weekday)"
          },
          "type": {
            "type": "string",
            "enum": ["flow", "routine"],
            "description": "Target type to trigger"
          },
          "targetId": {
            "type": "string",
            "description": "Flow ID or routine assignment ID"
          }
        }
      }
    },
    "rewards": {
      "type": "array",
      "description": "Redeemable rewards with star costs",
      "items": {
        "type": "object",
        "required": ["id", "title", "icon", "cost"],
        "additionalProperties": false,
        "properties": {
          "id": {
            "type": "string",
            "pattern": "^[a-zA-Z0-9-_]+$",
            "description": "Unique reward identifier"
          },
          "title": {
            "type": "string",
            "minLength": 1,
            "description": "Display name for reward"
          },
          "icon": {
            "type": "object",
            "required": ["type", "value"],
            "additionalProperties": false,
            "properties": {
              "type": {
                "type": "string",
                "enum": ["emoji", "image"],
                "description": "Icon type: emoji or image (uploaded filename)"
              },
              "value": {
                "type": "string",
                "minLength": 1,
                "description": "Emoji character or uploaded filename (auto-resolves to /uploads/)"
              }
            }
          },
          "cost": {
            "type": "integer",
            "minimum": 1,
            "description": "Star cost to redeem"
          }
        }
      }
    }
  }
}
